<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= series.title %> - Vol <%= volume.index %></title>
  <link rel="stylesheet" href="/views/public/styles/fonts.css">
  <link rel="stylesheet" href="/views/library/library.css"> 
</head>
<body class="library-body">
    <a href="/library/series/<%= series._id %>" class="library-back-link">&larr; Back to Volumes</a>
    
    <div class="library-container">
        <h1 class="library-title"><%= series.title %></h1>
        <h2 class="library-subtitle">Volume <%= volume.index %>: <%= volume.title %></h2>
        
        <div class="library-list">
            <% volume.chapters.forEach(chap => { %>
                <a href="/viewer?id=<%= volume._id %>&chapter=<%= chap.chapterNumber %>" class="library-item">
                    <div>
                        <div class="library-item-title">Chapter <%= chap.chapterNumber %></div>
                        <div class="library-card-meta"><%= chap.title || "Untitled" %></div>
                    </div>
                    <div class="library-item-icon">▶</div>
                </a>
            <% }) %>
        </div>
    </div>
</body>
</html>